# YuE2 Arm A: paired-error GAN on the edit critic, trained with the recipe's
# loss, critic optimizer and critic penalty (the package default).
arm: particle
steps: 2500
batch_size: 256
checkpoints: [250, 1000, 2500]
device: cuda:1
out_dir: results/gym/lunar_lander_particle_finetune/particle
imitation_weight: 0.0
real_encoding_weight: 0.0
synthetic_reconstruction_weight: 0.0
adv_weight: 1.0
train_scope: control
error_tokens: 8
error_width: 48
error_heads: 4
