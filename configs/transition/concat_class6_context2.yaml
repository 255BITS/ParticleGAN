# Intermediate class gain; all other settings match class 8 / context 2.
architecture: branches
width: 128
num_particles: 1024
critic_mode: joint
d_conditioning: concat
g_class_scale: 6.0
g_context_scale: 2.0
out_dir: results/transition/conditioning/branches_concat_class6_context2

# Explicit historical factors; independent of the current example defaults.
encoder: false
shared_state_critic: false
