# Intermediate geometry/time gain; all other settings match context scales 1 and 4.
architecture: branches
width: 128
num_particles: 1024
critic_mode: joint
d_conditioning: concat
g_class_scale: 8.0
g_context_scale: 2.0
out_dir: results/transition/conditioning/branches_concat_class8_context2

# Explicit historical factors; independent of the current example defaults.
encoder: false
shared_state_critic: false
