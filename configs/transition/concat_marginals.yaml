architecture: branches
width: 128
num_particles: 1024
critic_mode: joint_marginals
d_conditioning: concat
marginal_width: 128
marginal_weight: 1.0
out_dir: results/transition/conditioning/branches_concat_marginals

# Explicit historical factors; independent of the current example defaults.
encoder: false
g_class_scale: 1.0
shared_state_critic: false
