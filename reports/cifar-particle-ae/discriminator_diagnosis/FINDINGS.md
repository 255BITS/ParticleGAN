# D-only findings

The frozen feature representation is not obviously incapable of separating this generator from CIFAR. At the CNN E-only10k checkpoint, held-out AUC is0.5255. Holding G/prior fixed and continuing only the existing D for2048 steps improves AUC to0.9194 with the original regularization. Test/train scores0.9194/0.9202 agree. Pixel branch AUC rises0.5394→0.8896; combined pretrained feature heads0.4954→0.8762. These scores describe the components learned jointly, not independently trained branch ablations.

At the original50k checkpoint, held-out AUC0.4588 and image gradient0.0534 (vs0.3531 at10k), G adversarial gradient0.0575 (vs0.3735) and prior gradient0.00139 (vs0.01017). There is weak and somewhat inverted ranking on these live-weight draws. This supports unreliable D feedback in the joint trajectory, not a proof that it alone causes FID. FID measurements use EMA, while these probes use live G/D/prior.

Current-regularization D-only adaptation raises G gradient0.3735→11.1698 and prior gradient0.01017→0.12587. Larger gradients alone are not evidence of better generation; a joint FID test is necessary. A frozen-target discriminator has an easier learning problem than joint training.

Weaker bcap coefficient0.1 with lazy8 reaches test/train AUC0.9552/0.9552, input gradient1.2606, G gradient13.9328. Current coefficient1 every step reaches0.9471/0.9446, input gradient1.0744, G gradient13.1588. Training times47.7s/159.5s respectively vs48.6s forcurrent. Equal nominal average regularization does not produce equal optimization trajectories.

Bcap is one-sided above cap1. At both original checkpoint probes, no sampled real/fake total input norms exceed1: the penalty is locally inactive there. This does not establish inactivity throughout training. After2048 D-only updates, cap exceeds on real/fake12.2%/5.8% forcurrent and97.8%/97.3% forweaker. These are post-update fixed-panel diagnostics; lazy arms are measured immediately after regularized steps. Periodic within-cycle shocks remain a possible mechanism.

Pixel/features image-gradient cosine is approximately0 at both original checkpoints and small positive afteradaptation, not strong opposition. Total norm/sum of component norms stays0.74–0.78. This is ordinary nonaligned vector addition and does not support severe pixel/feature cancellation as the main explanation; parameter-gradient conflicts between objectives were not measured here.

Decision: jointly continue unchanged control, weaker coefficient (no D warmup), and current-reg D warmup (original coefficient thereafter). Warmup tests whether a one-time discriminator head start persists; coefficient tests sustained regularization relief. Use the same original10k parent and training RNG. No backbone replacement, fresh-head classifier, or encoder/prior intervention is needed before this test because the existing branches already recover separation. Every-step remains a possible later FID intervention, deferred because it costs more and weaker coefficient learns at least as much separation in this diagnostic.

Limitations:2048 held-out real images and2048 generated draws per evaluation, fixed across arms; no seed sweep. Train split diagnostic can include examples encountered in training. No test images are trained on. Parent G/prior is fixed during D-only, so D-only FID cannot improve. Full source/parent hashes, frozen state, nonmutating probes, and D Adam counters verified. Early failed exact replay test lacked deterministic-algorithm enforcement; with deterministic kernels and fixed pooling in the test only, original/new full-state continuation is bitwise equal. Production keeps historical CUDA settings.

## Follow-up probes during joint validation

Parameter-space branch decomposition used the derivative of the actual combined GAN loss, rather than separate branch losses. Across 16 batches, pixel/features cosine in G was −0.0166 at original10k, +0.1366 at original50k, −0.0138 at control20k and −0.0234 at weaker20k. Prior cosines were small positive. There is no evidence of strong branch cancellation after projection through G either. FP32 decomposition relative errors were below about4e-6 on average; an initial TF32 run tripped the strict1e-4 additive consistency check (error0.000367), so this read-only calculation disabled TF32. Model weights and training settings were not changed. See PROJECTED_GRADIENTS.json.

Eight-update game traces at20k, using the same512 held-out real images and fixed prior ID/noise draws at each phase, show the expected adversarial response on every observed update. Control mean AUC: beforeD0.4570, afterD0.5526, afterG/prior0.4614. Weaker:0.5354→0.5762→0.5253. Each D update increases ranking and each subsequent G/prior update decreases it (8/8 in both traces). This is normal GAN objective behavior and explains why checkpoint-phase classification can look weak; it does not by itself establish an unhealthy update ratio or explain the FID plateau.

A separate matched eight-step counterfactual trace isolates the generation-side movement. Starting with the actual post-D model, temporarily roll back onlyG or onlyprior after the real joint Adam step, evaluate, then restore both states before the next step. Mean AUC afterD0.5524, prior-only0.5519, G-only0.4616, both0.4613. Immediate ranking erasure is predominantly the G parameter update on these checkpoints, not movement of particle centers. The actual observed update components are used; gradients are not recomputed for a different objective. Counterfactual readouts preserve all states before training resumes. See UPDATE_COMPONENTS.json. These short instrumented artifacts have no comparable FID or runtime benchmark.

Read-only joint probes: at20k control AUC0.4134, fake input gradient0.4556, G gradient0.3315; weaker AUC0.5575, input gradient0.6394, G gradient0.2810. Better classification and larger image gradients did not imply a larger useful G update or materially better FID. After8 joint warmstart updates AUC remains0.9389, G gradient4.7574 (control8 AUC0.4707, G gradient0.4244). Warmup initially persists; final joint validation is needed to establish its longer-term effect.

Sparse training logs confirm bcap is sometimes active away from endpoint probes: nonzero penalties in10/50 logged regularization steps forcontrol and28/50 forweaker. Logs sample mostly every200 updates, so these are not full-training activation frequencies. See LOGGED_PENALTIES.json.


## Final joint result

All three10k→20k continuations certified. Final FID50k: weaker19.7848, control20.0119, D warmup25.2598. Weaker/control ranking reverses between15k and20k; no meaningful plateau breakthrough. Warmup final held-out AUC0.4322 and G gradient0.0589 show its initial stronger feedback was not sustained. Final G branch cosine−0.0647 and prior cosine+0.0163 still do not support strong branch cancellation. Detailed conclusions and next G-only learning-rate recommendation are in ../discriminator_joint/FINDINGS.md. No long promotion or new training is queued.
