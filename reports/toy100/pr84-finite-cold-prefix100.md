# Repaired PR84 cold ring: exact first-100-update replay

The finite-trial critic-refinement method still fails the fixed-target cold ring: its completed 1,200-update run ended with 3/8 modes, HQ 0.451416, and 0/24 passing native checkpoints. This separate diagnostic replays only updates 1–100, preserving the original 1,200-update host recipe and noise horizon. It does not change or qualify the candidate.

The source-bound replay compared **all 100 untimed update records and all 100 untimed critic-fit records exactly** with the completed run. Its only native evaluations were at steps 50 and 100, and both full evaluation rows matched exactly. The 16 passive stage snapshots at updates 1, 25, 50, and 100 each passed an RNG-invariance check. A complete post-checkpoint-100 model, D/G Adam, prior, EMA, data stream, and noise-policy state is saved for bounded causal tests. Actual D and G Adam calls and moment steps were 100 each; applied rates remained D 0.00425, G 0.00425, prior 0.0085. The original noise horizon remained 1,200, with 100 calls to its step clock. No dense evaluation was inserted.

At native step 50 the run had 0 covered modes and HQ 0. At step 100 it had 3 covered modes and HQ 0.336182. A read-only evaluation of the saved clean `G(prior.z)` at step 100 assigns exactly four particles each to the nearest target modes 0, 1, and 7, with none nearest the other five modes. The complete reference run's 24 sparse support observations preserve that same assignment from step 100 through 1,200. These target-center assignments are post hoc diagnostics; the training update and fit never select using them. The snapshot gives an exact early state for future local filters, but this replay alone does not identify a causal repair.

Evidence is in [continuous-evidence/pr84-finite-cold-prefix100](continuous-evidence/pr84-finite-cold-prefix100): `result.json` contains the exact comparison checks and source hashes, `prefix-records.json.gz` the first 100 raw rows, `prefix-states.pt.gz` all 17 saved states, and `source.tar.gz` the frozen driver and dependencies. `manifest.json` hashes each archived file and verifies every saved-state and source digest. The reference completed cold `mode_hold.json` has SHA-256 `754009f83104f935a33c4bd73d53668c6de64c1b6ae2efe276d10dd32ee6c4ab`; the replay compares against those exact bytes. The uncompressed state sidecar's SHA-256 is `002b3019c42c77414cb4f853db7818bba79bffab095f0d19052e4517ff45392d`.

The executable driver is [pr84_finite_cold_prefix_capture.py](pr84_finite_cold_prefix_capture.py), frozen at commit `b42b327d9b1bfa8008f2863bc1f4f400257f09b7`. Its source check must pass before it creates the declaration and starts training; it raises a sentinel only after the original checkpoint at update 100. The first direct-script startup attempt imported before putting the repository on `sys.path` and executed no training or declaration. That was fixed before the sole 100-update replay and is absent from its frozen source archive.
