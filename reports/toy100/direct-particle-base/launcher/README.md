These are the workspace launcher scripts used for the next round. They run from the shared workspace containing ParticleGAN-selected-h-stability-base and gan-attempts, not from this evidence subdirectory. launch-gan-width.py selects the three lanes; try-gan.sh starts fresh Astra/max sessions from the committed selected base. monitor-gan.py follows gan-attempts/current-batch.txt. The copied scripts preserve the exact launch configuration for review.
