No formulation passed recovery. **DI2** is the strongest partial lead, and K3P stays the selected base.

DI2 keeps the stationary ring and then actually reopens after the data change. Hold was 1200/1200 and the 300-update extension was 300/300, with the same minima as the parent run (HQ 0.9072265625 and 0.98779296875). Stationary checks were 5/5 and the pre-shift hold was 120/120. Deadline recovery was **77/81**. The four misses are steps 3570–3600, so there is no stable suffix and the gate is FAIL. Live recovery never reached the unconfirmed state, so the frozen control was not started. Transfer gates, the 22-toy matrix, extra native seeds, and the 30000-update protocol were not run.

The detector scores ordinary real minibatches: batch-mean residual over the reference standard deviation divided by sqrt(batch size). It does not read the budget, scores, targets, or the shift time. Five consecutive batches with max |z| ≥ 3.5 open a 0.20 rate multiplier for 200 steps, then the rate closes. On the stationary ring the score peaked at 3.49 and never latched, so learning rates and critic mixing stayed on the K3P schedule. After the shift the score reached 6.60, the latch started at penalty call 2429, and critic/generator learning rate rose to 0.00085 while mixing weight `s` stayed at 0. Modes were back to 8 with HQ ≥ 0.99 from step 2800 through 3560. The last four checks fell to 7 modes while the data score was already back near 1. That tail is generator-side drift. Real-batch innovation cannot see it.

DI1 saw the shift but only nudged the network multiplier to about 0.022, and recovery was 33/81. DI3 copied the critic into the anchor when the latch ended. That snap made recovery worse: 45/81. Horizon-based K3P noise and the learning-rate schedule are still in all three candidates. They are labeled intermediates, not horizon-independent results.

Details, traces, and replay commands are in `result.md` next to this attempt, with the six gate rows in `tests.jsonl`.
