The ring evaluator can now run the pinned stress protocol and the 30,000-update protocol on one uninterrupted host state. This lane did not train a candidate. K3P stays the selected base, and RP1 was not retrained.

**10/10 harness gates passed.** The 9,000-update stress run, the 30,000-update continuation, and RP1 qualification are **NOT_RUN**. The 3-update CUDA smoke reports quality **FAIL** (0 modes, HQ 0) with `qualification: false`. That smoke checks the evaluator only.

The frozen ring host still performs every gradient and Adam step. `benchmarks/toy100/continuous_qualification.py` (sha256 `01c7306aa8f717fcc29047804a81c0bcd931eed81a6daf1d8f524d7a48ccd119`) adds the protocol windows, absolute target positions, optimizer accounting, and a pre-shift witness. Stress is **707** checks (stable 5+480+60, deadlines 81+81). The long run keeps that prefix and adds **2,061** extension checks. `absolute_offset` is copied onto the original ring: after update 6,000 the target is `[1, 0]`, after 7,800 it is `[1, 1]`, and after 27,000 it is `[0, 1]`. Adding those vectors would end at `[2, 2]`.

The episode length is not given to the controller or the noise policy. Both stay on the canonical **1,200**-update horizon. The smoke used the K3P config (`total_steps` 7,000, network cap 1,600) for a 3-update episode and still measured controller horizon 1,200 and noise horizon 1,200. D and G each took 3 delegated Adam steps, both post-shift steps moved parameters, and measurement left the CPU, CUDA, and host-stream RNGs unchanged.

A witness gets `identity_sha256` only when model, Adam, host EMA, controller, latent stats, response history and prior membership, RNG, and the pre-shift ring are all present, keyed by `optimizer/group/index` rather than Python ids. The bare host smoke is missing controller, latent stats, and response, so its identity hash is null (`partial_sha256` `e9c048d68f084329ee908387b4b1343d259e4959af4f5cc2614b58643bbe0af0`). A missing candidate file, hash, or required attribute stops before training.

Ledger: `tests.jsonl`. Details: `result.md` and `reports/toy100/continuous-round-3/qualification-harness/README.md`. Replay the CPU checks with `python -m pytest tests/test_continuous_qualification.py -q`, and the CUDA smoke with `--smoke-host` under the assigned GPU and `CUBLAS_WORKSPACE_CONFIG=:4096:8`. A later candidate uses `--protocol stress` or `--protocol long-term` after `--check-candidate`.
