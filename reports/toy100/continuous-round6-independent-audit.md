# Round 6 saved-state and whole-map audit

The one-step virtual-critic G response did not clear its frozen saved-state filter. I independently regraded the archived 44 checkpoints: 12/12 pass for updates 1324–1335, 15/16 for 1380–1395 (1395 fails at HQ .79834), and 1/16 for 1530–1545 (minimum seven modes/HQ .81470). The ordinary arm's 44 support arrays and 44 update records match the original capture exactly. Eleven available full accepted states match their captured hashes; these include the first state of each of the three branches. The other 33 accepted states have archived hashes but no independent captured full-state comparison. Both arms use constant D/G/prior rates .00425/.00425/.0085, one Adam moment update per role per outer step, and equal branch RNG/noise endpoints. This is a saved-state diagnostic failure; no warm or cold host gate ran for the candidate. The compact [independent receipt](continuous-evidence/round6-independent/unroll-saved44/audit.json), [source snapshot and compressed raw](continuous-evidence/round6-independent/unroll-saved44/manifest.json), and [verifier](pr84_unroll_saved44_independent_audit.py) preserve the exact limits of that claim.

The separate whole-map filter replaces the entire neural G move with the exact fixed-assignment minimizer of sampled, unit-mean bidirectional Chamfer `C+Q`; it therefore removes preceding GAN damage and latent pullback error. Its warm1324 cloud passes 100/100 late-noise checks. Both cold-start clouds pass 0/100 and finish at four modes: cold1 HQ .830566, cold100 HQ .830078. From the three saved model states and their cloned data streams, the [independent replay](pr84_whole_map_independent_audit.py) regenerated **300** real minibatches and exactly reproduced all saved target clouds, assignment counts, sampled objectives and fixed 4096-draw grades. Frozen [source, declaration and compressed raw](continuous-evidence/round6-independent/whole-map/manifest.json) retain the original bytes. Source SHA checks bind the G architecture, sampler, `chamfer_targets`, fixed evaluator and driver. The filter evaluates each cloud with the *late* .029 output-noise law and consumes only real minibatches from each saved stream; its batches after the first are not a replay of the native GAN's data-stream chronology. This is a free-output geometry rejection filter, not a production cold-training or long-hold claim.

The failure is a local assignment basin, not failure of the sampled `C+Q` objective to prefer eight modes. At cold1 update 100, one particle receives 25 sampled real points from mode 4 and 14 from mode 5 and lies 1.137 from its nearest diagnostic ring center; another receives 16 from mode 2 and 12 from mode 3 and lies .772 away. Six particles sit by one already-covered mode, two receiving no real points. Cold100 has analogous mixed 16/16 and 13/16 assignments, with bridge points .889/.967 from the nearest diagnostic centers. Fixed-assignment averaging cannot move these bridge points across the partition boundary, and the backward `Q` anchor keeps empty-cell particles near sampled real points in the already-covered region. The recorded sampled objective falls at every target update, while the four-mode structure persists.

As an offline data-only escape test, [the helper](chamfer_discrete_reallocation.py) enumerates every one-particle donor and observed real row, then chooses the exact largest same-batch `C+Q` decrease. There are no mode centers, quality scores, random draws, schedule, or tuned coefficient in the selection. On each *fixed final real batch*, four strictly decreasing relocations reach eight modes: cold1 `C+Q` .831181→.008422 and fixed-noise HQ 1.000000; cold100 .780382→.006563 and HQ .999512. Post-hoc center labels explain the moves, but do not select them. Three focused tests check global best selection against brute force, objective descent, state/RNG non-mutation, and input guards. Later small same-batch improvements can chase noisy samples and lower fixed-noise HQ; the four-move observation does not specify a safe training cadence. A neural candidate would have to realize each discrete output proposal through G/prior, accept it on recomputed actual sampled `C+Q`, and then pass warm, cold acquisition and same-target hold gates. No such neural result is established here.
