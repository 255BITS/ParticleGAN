# Lane 1: autonomous reversible precision

Use PX3's measured retention/recovery tradeoff as a mechanism lead, not an
eligible base or inherited pass. Its reference-gap motion rule reached a stable
shifted ring, but retained horizon-based noise and an early/late cold transition.
Its calm latch closed during the first lull while the smoothed gap was still
rising. Declare a successor that determines precision from ordinary game/update
signals, can reopen later, and removes ALL planned-horizon coupling, including
noise and the API's total-step limit. Implement it in the public KA2 API base;
porting a PX3 idea does not imply parity with its old research host.

Investigate excursion growth/contraction and model-update evidence so temporary
lulls do not end acquisition prematurely. Do not repeat PX1's half-peak cutoff
or PX2's proportional-gap rate unchanged. Do not assume critic surprise alone
distinguishes changed data from self-induced collapse. Other lanes own a
separate data-drift detector and fixed-rate memory repair; keep your hypothesis
distinct. Log opening, closing and later reopening, not just min/max rates.

Read-only lead:
`reports/toy100/continuous-round-3/completed-prox/attempts/k3p_prox_release/result.md`
in the supplied research evidence checkout. Its old deadline-based verdict and
instruction to defer removing horizon noise are superseded by COMMON.md.
