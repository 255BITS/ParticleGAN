# Isotropic ×24 two_broad: published absolute lengths vs lengthscale-free MLP

Distinct from PR#45 (which used gauge-matched batchfeat kernels×32 + init×32 as control).
Here the control is SimpleMLP with matched init_std only — no absolute RBF lengths.
