# Completed-scout follow-up

All16 scouts completed with zero failures. Only proposal_mixed_pair25 meets the
predeclared continuous-quality extension gates at both prefixes. Continue its
exact2k checkpoint to5k on the unchanged10k schedule. No architecture/objective
or RNG reset. This is a diagnostic continuation; all primary scout passes are0.

A second, adaptive2k scout uses exactly the same settings without the proposal
adapter: plain_mixed_pair25. This adds the missing matched training control for
the best combination. Evaluation bypass only modestly reduces warm Q in the
winner, so a trained control is needed to interpret repair's contribution. This
scout was selected AFTER initial results, not part of the predeclared16.

Both jobs use the same source and shared log. Do not inspect before completion.
At most one additional5k extension remains available if the matched scout meets
the original plan's gates; do not extend a regression merely to occupy a GPU.
